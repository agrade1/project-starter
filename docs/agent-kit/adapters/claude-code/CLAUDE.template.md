# 대상 프로젝트 CLAUDE.md에 병합할 조각

이 파일 자체는 자동 로딩 지침이 아니다. 아래 블록을 대상 루트 `CLAUDE.md`에 병합한다. 기존 파일을 대체하지 않는다. 정본 경로는 대상 프로젝트에 맞춘다.

```md
## 전문 역할 작업 지침

작업 시작 시 `docs/agent-kit/common/START_HERE.md`와
`docs/agent-kit/common/WORKING_RULES.md`를 읽는다.
선택한 역할·기능·워크플로만 추가로 읽으며 전체 킷을 자동 import하지 않는다.
요구사항·진행·정본 경로는 `docs/project/PROJECT.md` 또는 기존 프로젝트 정본에서 확인한다.
기존 코드·지침·사용자 변경을 보존하고 승인된 범위의 결과·검증·인계까지 완료한다.
구현자의 자기평가로 독립 리뷰·실제 검증을 대체하지 않는다.
원격 쓰기·출시·실결제는 이 킷의 기본 업무 범위 밖이다.
```
